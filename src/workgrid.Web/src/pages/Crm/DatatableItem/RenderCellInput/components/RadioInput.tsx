import { InputProps } from "reactstrap";

/**
 * RadioInput — tekil boolean veya seçenekli radio girişi.
 *
 *  Seçenek yoksa tek boolean kutu ("*1*"/"*0*"); seçenek varsa ",*," ile
 *  ayrılmış listeden radio seçtirir (aynı değere tekrar tıklama seçimi kaldırır)
 *  ve değeri forma/handleChange'e yazar. Serileştirme mantığı gizlenmiştir.
 */
export const RadioInput = (_props: InputProps): JSX.Element => {
  throw new Error("Source available on request.");
};
