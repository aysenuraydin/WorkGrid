import { InputProps } from "reactstrap";

/**
 * CheckboxInput — tekil boolean veya çoklu-seçim checkbox girişi.
 *
 *  Seçenek yoksa tek boolean kutu ("*1*"/"*0*"); seçenek varsa ",*," ile
 *  ayrılmış çoklu seçim yönetir (işaretlenen değerler bu ayraçla birleştirilip
 *  forma/handleChange'e yazılır). Boyut ve salt-okunur durumları uygulanır.
 *  Serileştirme mantığı gizlenmiştir.
 */
export const CheckboxInput = (_props: InputProps): JSX.Element => {
  throw new Error("Source available on request.");
};
