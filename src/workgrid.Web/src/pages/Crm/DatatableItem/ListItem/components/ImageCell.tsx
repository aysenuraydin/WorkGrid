interface CellProps { val: string; colClass: string; isIcon: boolean; }

/**
 * ImageCell — görsel değerini ViewImage önizleyicisiyle gösterir. 
 *  Değeri liste modunda ViewImage bileşenine devreder. Gizlenmiştir. 
 */
export const ImageCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
