interface CellProps { val: string; colClass: string; clssnm: string; isIcon: boolean; }

/**
 * TextareaCell — zengin metin/HTML değerinin düz-metin önizlemesini gösterir. 
 *  HTML etiketleri sıyrılıp elde edilen düz metin belirli bir karakter sınırına
 *  göre kısaltılarak gösterilir. Etiket-sıyırma/önizleme mantığı gizlenmiştir. 
 */
export const TextareaCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
