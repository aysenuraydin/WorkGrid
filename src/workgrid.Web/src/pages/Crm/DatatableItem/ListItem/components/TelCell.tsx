import { IInputProps } from 'hooks/useCellLogic';
interface CellProps { val: string; colClass: string; inputProps: IInputProps; }

/**
 * TelCell — telefon değerini ikon + prefix ile gösterir. 
 *  Telefon ikonu, yapılandırılmış prefix ve değer (uzunsa kırpılmış) gösterilir.
 *  Gizlenmiştir. 
 */
export const TelCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
