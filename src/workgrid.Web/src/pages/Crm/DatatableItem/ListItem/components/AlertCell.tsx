interface CellProps { val: string; colClass: string; }

/**
 * AlertCell — kod`mesaj formatındaki değeri renkli/ikonlu uyarı olarak gösterir.
 *
 *  Değerin `\`` ile ayrılan ilk parçası (0–7) bir renk + ikon eşlemesine
 *  çevrilir, ikinci parça mesaj olarak reactstrap Alert içinde gösterilir.
 *  Gizlenmiştir.
 */
export const AlertCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
