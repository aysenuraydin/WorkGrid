interface CellProps { colClass: string; }

/**
 * PasswordCell — şifre değerini gizli (kilit + noktalar) olarak gösterir. 
 *  Gerçek değeri hiç yazmaz; sabit bir kilit ikonu ve maske noktaları render
 *  eder. Gizlenmiştir. 
 */
export const PasswordCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
