interface CellProps { val: string; colClass: string; clssnm: string; }

/**
 * DateCell — tarih değerini takvim ikonuyla, T ayıracını boşluğa çevirerek gösterir. 
 *  ISO tarihteki "T" boşlukla değiştirilir; başına takvim ikonu eklenir.
 *  Gizlenmiştir. 
 */
export const DateCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
