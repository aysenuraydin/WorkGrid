/**
 * ParentCell — ilişkili (parent) satır id'sini etikete çevirip gösterir. 
 *  NE YAPAR (özet):
 *  Hücredeki rowId, hedef tablonun ilişki tanımından (foreignTablesFk →
 *  createOrUpdateColumnId) bulunan etiket kolonunun değeriyle eşlenip gösterilir;
 *  bulunamazsa "#id" düşer. İlişki-tabanlı etiket çözümü gizlenmiştir. 
 */
export const ParentCell = (_props: { val: string; colClass?: string; col?: any; [key: string]: any }): JSX.Element | null => {
  throw new Error("Source available on request.");
};
