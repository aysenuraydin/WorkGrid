interface CellProps { val: string; colClass: string; isIcon: boolean; }

/**
 * RangeCell — sayısal değeri salt-okunur range çubuğu + etiketle gösterir. 
 *  Değer bir range input'una (readOnly) ve yanında rozet olarak yazılır.
 *  Gizlenmiştir. 
 */
export const RangeCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
