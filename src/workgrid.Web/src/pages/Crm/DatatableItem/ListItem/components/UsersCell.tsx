/**
 * UsersCell — kullanıcı id listesini avatar + isim yığını olarak gösterir. 
 *  NE YAPAR (özet):
 *  Virgülle ayrılmış kullanıcı id'leri, kullanıcı listesinden eşlenip profil
 *  fotoğrafı (yoksa isim baş harfleri + türetilmiş renk) ile üst üste binen
 *  avatar yığını olarak render edilir. Id→üye çözümü ve avatar üretimi
 *  gizlenmiştir. 
 */
export const UsersCell = (_props: { val: string; colClass?: string; [key: string]: any }): JSX.Element | null => {
  throw new Error("Source available on request.");
};
