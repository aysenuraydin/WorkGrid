/**
 * SwitchCell — boolean değeri salt-okunur anahtar (switch) olarak gösterir. 
 *  "*1*" değeri açık kabul edilir; devre dışı bir switch input çizilir.
 *  Gizlenmiştir. 
 */
export const SwitchCell = (_props: { val: string; colClass?: string; [key: string]: any }): JSX.Element => {
  throw new Error("Source available on request.");
};
