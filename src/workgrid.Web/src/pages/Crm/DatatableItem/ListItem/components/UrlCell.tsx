interface CellProps { val: string; colClass: string; }

/**
 * UrlCell — değeri tıklanabilir bağlantı olarak gösterir. 
 *  Değer yeni sekmede açılan bir link olarak render edilir; uzun URL kırpılır.
 *  Gizlenmiştir. 
 */
export const UrlCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
