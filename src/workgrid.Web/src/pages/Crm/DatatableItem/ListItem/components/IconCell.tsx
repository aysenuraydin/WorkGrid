/**
 * IconCell — değeri bir ikon sınıfı olarak yorumlayıp ikonu gösterir. 
 *  Değer (ör. "ri-user-line") doğrudan ikon class'ı olarak render edilir.
 *  Gizlenmiştir. 
 */
export const IconCell = (_props: { val: string; colClass?: string; [key: string]: any }): JSX.Element | null => {
  throw new Error("Source available on request.");
};
