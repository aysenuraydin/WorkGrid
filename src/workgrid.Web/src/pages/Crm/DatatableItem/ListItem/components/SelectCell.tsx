interface CellProps { val: string; colClass: string; clssnm: string; isIcon: boolean; }

/**
 * SelectCell — ",*," ile ayrılmış seçim değerlerini rozet olarak gösterir. 
 *  Çoklu seçim değerleri ayrıştırılıp her biri rozet olarak çizilir; uzun
 *  değerler kırpılır. Gizlenmiştir. 
 */
export const SelectCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
