interface CellProps { val: string; colClass: string; }

/**
 * BadgeCell — kod`etiket değerini renkli rozet olarak gösterir.
 *
 *  Değerin `\`` ile ayrılan ilk parçası (0–7) bir Bootstrap renk sınıfına
 *  çevrilir, ikinci parça rozet metni olur. Gizlenmiştir.
 */
export const BadgeCell = (_props: CellProps): JSX.Element => {
  throw new Error("Source available on request.");
};
