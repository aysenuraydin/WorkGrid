export enum ValidationRuleEnum {
    required = "required",
    email = "email",
    url = "url",
    min = "min",
    max = "max",
    minLength = "minLength",
    maxLength = "maxLength",
    matches = "matches",
    allowedValues = "allowedValues",
    unique = "unique",
    integer = "integer",
    positive = "positive",
    negative = "negative",

    length = "length",
    pattern = "pattern",
    trim = "trim",
}