

export enum AttributeEnum {
    options="options",
    multiple="multiple",
    rows="rows",
    cols="cols",
    step="step",
    accept="accept",
    maxSizeMB="maxSizeMB",
    format="format",
    rangeLimit="rangeLimit",

    required = "required",   
    autoFocus = "autoFocus", 
    label = "label",
}